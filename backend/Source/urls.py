from django.urls import path
from.import views

urlpatterns = [
    path("stock/<str:stock>/",views.source.as_view(),name="source"),
    path("stock/data/<str:stock>/",views.Data.as_view()),
    path("history/",views.History.as_view()),
]












































































































